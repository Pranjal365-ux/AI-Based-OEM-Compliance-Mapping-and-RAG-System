# Core
pydantic>=2.0
chromadb>=0.4
pymupdf>=1.23          # imported as `fitz`
pdfplumber>=0.10
openai>=1.0
loguru>=0.7
python-dotenv>=1.0
requests>=2.31
streamlit>=1.30
Pillow>=10.0

# Optional — scanned/image-only PDF support (OCR path in ingestion/pdf_extractor.py)
# Safe to omit if you only ingest text-based PDFs.
opencv-python-headless>=4.8
numpy>=1.24
paddleocr>=2.7

# Runtime services this project talks to (not pip-installable, see README):
#   - Ollama, serving an embedding model (bge-m3) and one or more chat models
#   - Optional: a hosted OpenAI-compatible endpoint (e.g. Groq) for the compliance model
